spring:
  application:
    name: journalApp
  data:
    mongodb:
      uri: ${MONGODB_URI}/journaldb
      auto-index-creation: true
  config:
    import: optional:application-secret.yml
  mail:
    host: smtp.gmail.com
    port: 587
    username: adityamathur192@gmail.com
    password: ${APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true


server:
  port: 8080